# Agent Runtime — 运行层

本目录定义 Agent **如何工作**，不存放易变业务数字。

| 文件 | 何时修改 |
|---|---|
| `AGENT.md` | 角色、原则、边界变化 |
| `ROUTING.md` | 新问题类型或知识目录调整 |
| `OUTPUT_TEMPLATES.md` | 输出结构需要统一变更 |

业务事实请改 `../knowledge/`，不要写进这里。
